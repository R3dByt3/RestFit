<UserControl x:Class="FitnessTrackingAndPlanning.Views.FriendsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FitnessTrackingAndPlanning.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="Background" Value="DeepPink"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Background="DimGray">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <ComboBox Grid.Row="0" Margin="20"/>
            <Grid Grid.Row="1"  Margin="20 0 20 20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Label Grid.Column="0" Grid.Row="0" Content="Durchschnitt" FontSize="14" Foreground="White"/>
                <Label Grid.Column="0" Grid.Row="1" Content="Sätze:" FontSize="14" Foreground="White"/>
                <Label Grid.Column="1" Grid.Row="1" Content="3" FontSize="14" Foreground="White"/>
                <Label Grid.Column="0" Grid.Row="2" Content="Wiederholungen:" FontSize="14" Foreground="White"/>
                <Label Grid.Column="1" Grid.Row="2" Content="12" FontSize="14" Foreground="White"/>
                <Label Grid.Column="0" Grid.Row="3" Content="Gewicht" FontSize="14" Foreground="White"/>
                <Label Grid.Column="1" Grid.Row="3" Content="50kg" FontSize="14" Foreground="White"/>
            </Grid>
            <StackPanel Grid.Row="2" Background="DeepPink" Margin="20 0 20 20">
                <Label Content="Du schaffst das!" FontSize="14" Foreground="White"/>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Column="1" Background="#404040" Orientation="Vertical">
            <Label Content="Freunde" FontSize="14" Foreground="White" VerticalAlignment="Top"/>
            <Border BorderBrush="DimGray" BorderThickness="0 0 0 1" />
            <Label Content="Anne" FontSize="14" Foreground="White" />
            <Button Content="Bearbeiten" VerticalAlignment="Bottom" />
            <Button Content="Anfragen" VerticalAlignment="Bottom" />
            <Button Content="Hinzufügen" VerticalAlignment="Bottom" Margin ="0"/>
        </StackPanel>
    </Grid>
</UserControl>
